[project]
name = "transaction-reconcile"
version = "0.1.0"
description = "Multi-Source Reconciliation Agent for Razorpay AI Buildathon"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0.0",
    "faker>=20.0.0",
    "tabulate>=0.9.0",
    "python-dateutil>=2.8.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
